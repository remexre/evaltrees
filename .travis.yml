language: rust
sudo: false
rust:
- stable
- beta
- nightly
os:
- linux
- osx
matrix:
  allow_failures:
  - rust: nightly
  fast_finish: true
cache: cargo

script:
- cargo test
# Ignored unless the right platform is detected
- .travis/make_wasm.sh
